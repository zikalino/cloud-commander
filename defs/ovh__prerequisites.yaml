type: layout-form
id: root
form:
  #
  # Azure CLI
  #
  - type: fieldset
    platform: win32
    subitems:
      - $include: __git_install_step.yaml
        install: |
            cd ~
            git clone --recursive https://github.com/Azure/azure-rest-api-specs.git

footer:
  - type: footer
    id: footer
    subitems:
      - type: button
        id: install_button
        codicon: run-all
