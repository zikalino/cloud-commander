#
# Kamatera
#
- item-types: kamatera-server-list
  cmd: cloudcli server list --format json
  path-id: $[::].id
  path-name: $[::].name
  path-raw: $[::]
  item-type-generic: vm
  item-type: kamatera-server
