#--------------------------------------------------------------------------------------------------------------
# Autogenerated by SmarterGen - do not modify this file.
#--------------------------------------------------------------------------------------------------------------
# linode-cli volumes create
# Create a volume
# API Documentation: https://techdocs.akamai.com/linode-api/reference/post-volume
# 
# Example Usage: 
#   linode-cli volumes create \
#     --label my-volume \
#     --size 20 \
#     --linode_id 12346 \
#     --no-defaults
# 
# Arguments:
#   --label (required): The Volume's label, which is also used in the            
#   filesystem_path of the resulting volume.                                     
# 
#   --config_id: When creating a Volume attached to a Linode, the ID of the      
#   Linode Config to include the new Volume in.                                  
#   --linode_id: The Linode this volume should be attached to upon creation.
#   --region: The Region to deploy this Volume in.
#   --size: The initial size of this volume, in GB.
#   --tags: An array of Tags applied to this object.
# 
# 
type: layout-form
header: 
  - type: header
    title: linode volumes create
    logo: icon.webp
form:
  - type: fieldset
    subitems:
      - type: row
        subitems: 
          - type: textfield
            name: label (required)
            description: The Volume's label, which is also used in the filesystem_path of the resulting volume. 
            produces: 
              - variable: label
      - $include: __linode_regions_list_selector.yaml
      - type: row
        subitems: 
          - type: textfield
            name: config_id
            description: When creating a Volume attached to a Linode, the ID of the Linode Config to include the new Volume in.
            produces: 
              - variable: config_id
      - type: row
        subitems: 
          - type: textfield
            name: linode_id
            description: The Linode this volume should be attached to upon creation.
            produces: 
              - variable: linode_id
      - type: row
        subitems: 
          - type: textfield
            name: size
            description: The initial size of this volume, in GB.
            produces: 
              - variable: size
      - $include: __az_tags_list.yaml
      - type: 'action-row'
        expandable: false
        name: linode volumes create
        consumes:
          - variable: label
            parameter: --label
            required: true
          - variable: region
            parameter: --region
            required: true
          - variable: config_id
            parameter: --config_id
            required: false
          - variable: linode_id
            parameter: --linode_id
            required: false
          - variable: size
            parameter: --size
            required: false
          - variable: tags
            parameter: --tags
            required: false
        install: linode volumes create
        produces:
          - variable: output
      - type: 'output-row'
        data: output
        content: ...