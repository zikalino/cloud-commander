#--------------------------------------------------------------------------------------------------------------
# Autogenerated by SmarterGen - do not modify this file.
#--------------------------------------------------------------------------------------------------------------
# Creates a database cluster with the specified name.
# 
# You can customize the configuration using the listed flags, all of which are optional. Without any flags set, the command creates a single-node, single-CPU PostgreSQL database cluster.
# 
# Usage:
#   doctl databases create <name> [flags]
# 
# Aliases:
#   create, c
# 
# Examples:
# The following example creates a database cluster named `example-database` in the `nyc1` region with a single  1 GB node: doctl databases create example-database --region nyc1 --size db-s-1vcpu-1gb --num-nodes 1
# 
# Flags:
#       --engine pg                          The database's engine. Possible values are: pg, `mysql`, `redis`, `mongodb`, `kafka` and `opensearch`. (default "pg")
#   -h, --help                               help for create
#       --num-nodes int                      The number of nodes in the database cluster. Valid values are 1-3. In addition to the primary node, up to two standby nodes may be added for high availability. (default 1)
#       --private-network-uuid string        The UUID of a VPC to create the database cluster in. The command uses the region's default VPC if excluded.
#       --region nyc1                        The data center region where the database cluster resides, such as nyc1 or `sfo2`. (default "nyc1")
#       --restore-from-cluster-name string   The name of an existing database cluster to restore from.
#       --restore-from-timestamp string      The timestamp of an existing database cluster backup in UTC combined date and time format (2006-01-02 15:04:05 +0000 UTC). The most recent backup is used if excluded.
#       --size db-s-1vcpu-1gb                The size of the nodes in the database cluster, for example db-s-1vcpu-1gb indicates a 1 CPU, 1GB node. For a list of available size slugs, visit: https://docs.digitalocean.com/reference/api/api-reference/#tag/Databases (default "db-s-1vcpu-1gb")
#       --storage-size-mib int               The amount of disk space allocated to the cluster. Applicable for PostgreSQL and MySQL clusters. Each plan size has a default value but can be increased in increments up to a maximum amount. For ranges, visit: https://www.digitalocean.com/pricing/managed-databases
#       --tag strings                        A comma-separated list of tags to apply to the database cluster.
#       --version string                     The database engine's version, such as 14 for PostgreSQL version 14.
#       --wait                               A boolean value that specifies whether to wait for the database cluster to be provisioned before returning control to the terminal.
# 
# Global Flags:
#   -t, --access-token string   API V2 access token
#   -u, --api-url string        Override default API endpoint
#   -c, --config string         Specify a custom config file (default "C:\\Users\\Lenovo\\AppData\\Roaming\\doctl\\config.yaml")
#       --context string        Specify a custom authentication context name
#       --http-retry-max int    Set maximum number of retries for requests that fail with a 429 or 500-level error (default 5)
#       --interactive           Enable interactive behavior. Defaults to true if the terminal supports it (default false)
#   -o, --output string         Desired output format [text|json] (default "text")
#       --trace                 Show a log of network activity while performing a command
#   -v, --verbose               Enable verbose output
# 
type: layout-form
header: 
  - type: header
    title: doctl databases create
    logo: icon.webp
form:
  - type: section
    subitems:
      - type: row
        subitems: 
          - type: textfield
            name: <name> (required)
            description: name
            produces: 
              - variable: name
      - $include: doctl/__doctl_compute_region_list_selector.yaml
      - type: row
        subitems: 
          - type: dropdown
            name: engine
            description: "The database's engine. Possible values are: pg, `mysql`, `redis`, `mongodb`, `kafka` and `opensearch`. (default \"pg\")"
            items:
              - pg
              - mysql
              - redis
              - mongodb
              - kafka
              - opensearch
            produces: 
              - variable: engine
      - type: row
        subitems: 
          - type: textfield
            name: num-nodes
            description: The number of nodes in the database cluster. Valid values are 1-3. In addition to the primary node, up to two standby nodes may be added for high availability. (default 1)
            produces: 
              - variable: num_nodes
      - type: row
        subitems: 
          - type: textfield
            name: private-network-uuid
            description: The UUID of a VPC to create the database cluster in. The command uses the region's default VPC if excluded.
            produces: 
              - variable: private_network_uuid
      - type: row
        subitems: 
          - type: textfield
            name: restore-from-cluster-name
            description: The name of an existing database cluster to restore from.
            produces: 
              - variable: restore_from_cluster_name
      - type: row
        subitems: 
          - type: textfield
            name: restore-from-timestamp
            description: "The timestamp of an existing database cluster backup in UTC combined date and time format (2006-01-02 15:04:05 +0000 UTC). The most recent backup is used if excluded."
            produces: 
              - variable: restore_from_timestamp
      - type: row
        subitems: 
          - type: checkbox
            name: size
            description: "The size of the nodes in the database cluster, for example db-s-1vcpu-1gb indicates a 1 CPU, 1GB node. For a list of available size slugs, visit: https://docs.digitalocean.com/reference/api/api-reference/#tag/Databases (default \"db-s-1vcpu-1gb\")"
            val-false: undefined
            produces: 
              - variable: size
      - type: row
        subitems: 
          - type: textfield
            name: storage-size-mib
            description: "The amount of disk space allocated to the cluster. Applicable for PostgreSQL and MySQL clusters. Each plan size has a default value but can be increased in increments up to a maximum amount. For ranges, visit: https://www.digitalocean.com/pricing/managed-databases"
            produces: 
              - variable: storage_size_mib
      - type: row
        subitems: 
          - type: textfield
            name: tag
            description: A comma-separated list of tags to apply to the database cluster.
            produces: 
              - variable: tag
      - type: row
        subitems: 
          - type: textfield
            name: version
            description: The database engine's version, such as 14 for PostgreSQL version 14.
            produces: 
              - variable: version
      - type: row
        subitems: 
          - type: checkbox
            name: wait
            description: "A boolean value that specifies whether to wait for the database cluster to be provisioned before returning control to the terminal.  lobal Flags:"
            val-false: undefined
            produces: 
              - variable: wait
      - type: row
        subitems: 
          - type: textfield
            name: access-token
            description: API V2 access token
            produces: 
              - variable: access_token
      - type: row
        subitems: 
          - type: textfield
            name: api-url
            description: Override default API endpoint
            produces: 
              - variable: api_url
      - type: row
        subitems: 
          - type: textfield
            name: config
            description: "Specify a custom config file (default \"C:\\Users\\Lenovo\\AppData\\Roaming\\doctl\\config.yaml\")"
            produces: 
              - variable: config
      - type: row
        subitems: 
          - type: textfield
            name: context
            description: Specify a custom authentication context name
            produces: 
              - variable: context
      - type: row
        subitems: 
          - type: textfield
            name: http-retry-max
            description: Set maximum number of retries for requests that fail with a 429 or 500-level error (default 5)
            produces: 
              - variable: http_retry_max
      - type: row
        subitems: 
          - type: checkbox
            name: interactive
            description: Enable interactive behavior. Defaults to true if the terminal supports it (default false)
            val-false: undefined
            produces: 
              - variable: interactive
      - type: row
        subitems: 
          - type: textfield
            name: output
            description: Desired output format [text|json] (default "text")
            produces: 
              - variable: output
      - type: row
        subitems: 
          - type: checkbox
            name: trace
            description: Show a log of network activity while performing a command
            val-false: undefined
            produces: 
              - variable: trace
      - type: row
        subitems: 
          - type: checkbox
            name: verbose
            description: Enable verbose output 
            val-false: undefined
            produces: 
              - variable: verbose
      - type: 'action-row'
        expandable: false
        name: doctl databases create
        consumes:
          - variable: name
            required: true
          - variable: region
            parameter: --region
            required: true
          - variable: engine
            parameter: --engine
            required: false
          - variable: num_nodes
            parameter: --num-nodes
            required: false
          - variable: private_network_uuid
            parameter: --private-network-uuid
            required: false
          - variable: restore_from_cluster_name
            parameter: --restore-from-cluster-name
            required: false
          - variable: restore_from_timestamp
            parameter: --restore-from-timestamp
            required: false
          - variable: size
            parameter: --size
            required: false
          - variable: storage_size_mib
            parameter: --storage-size-mib
            required: false
          - variable: tag
            parameter: --tag
            required: false
          - variable: version
            parameter: --version
            required: false
          - variable: wait
            parameter: --wait
            required: false
          - variable: access_token
            parameter: --access-token
            required: false
          - variable: api_url
            parameter: --api-url
            required: false
          - variable: config
            parameter: --config
            required: false
          - variable: context
            parameter: --context
            required: false
          - variable: http_retry_max
            parameter: --http-retry-max
            required: false
          - variable: interactive
            parameter: --interactive
            required: false
          - variable: output
            parameter: --output
            required: false
          - variable: trace
            parameter: --trace
            required: false
          - variable: verbose
            parameter: --verbose
            required: false
        install: doctl databases create ${name}
        produces:
          - variable: output
      - type: 'output-row'
        data: output
        content: ''