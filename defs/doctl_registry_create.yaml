#--------------------------------------------------------------------------------------------------------------
# Autogenerated by SmarterGen - do not modify this file.
#--------------------------------------------------------------------------------------------------------------
# Creates a new private container registry with the provided name.
# 
# Usage:
#   doctl registry create <registry-name> [flags]
# 
# Examples:
# The following example creates a registry named `example-registry` in the NYC3 region: doctl registry create example-registry --region=nyc3
# 
# Flags:
#   -h, --help                                                          help for create
#       --region slug                                                   A slug indicating which datacenter region the registry reside in. For a list of supported region slugs, use the `doctl registry options available-regions` command
#       --subscription-tier doctl registry options subscription-tiers   Subscription tier for the new registry. For a list of possible values, use the doctl registry options subscription-tiers command. (required) (default "basic")
# 
# Global Flags:
#   -t, --access-token string   API V2 access token
#   -u, --api-url string        Override default API endpoint
#   -c, --config string         Specify a custom config file (default "C:\\Users\\Lenovo\\AppData\\Roaming\\doctl\\config.yaml")
#       --context string        Specify a custom authentication context name
#       --http-retry-max int    Set maximum number of retries for requests that fail with a 429 or 500-level error (default 5)
#       --interactive           Enable interactive behavior. Defaults to true if the terminal supports it (default false)
#   -o, --output string         Desired output format [text|json] (default "text")
#       --trace                 Show a log of network activity while performing a command
#   -v, --verbose               Enable verbose output
# 
type: layout-form
header: 
  - type: header
    title: doctl registry create
    logo: icon.webp
form:
  - type: section
    subitems:
      - type: row
        subitems: 
          - type: textfield
            name: <registry_name> (required)
            description: registry-name
            produces: 
              - variable: registry_name
      - $include: doctl/__doctl_compute_region_list_selector.yaml
      - $include: doctl/__doctl_registry_options_subscription_tiers_selector.yaml
      - type: row
        subitems: 
          - type: textfield
            name: access-token
            description: API V2 access token
            produces: 
              - variable: access_token
      - type: row
        subitems: 
          - type: textfield
            name: api-url
            description: Override default API endpoint
            produces: 
              - variable: api_url
      - type: row
        subitems: 
          - type: textfield
            name: config
            description: "Specify a custom config file (default \"C:\\Users\\Lenovo\\AppData\\Roaming\\doctl\\config.yaml\")"
            produces: 
              - variable: config
      - type: row
        subitems: 
          - type: textfield
            name: context
            description: Specify a custom authentication context name
            produces: 
              - variable: context
      - type: row
        subitems: 
          - type: textfield
            name: http-retry-max
            description: Set maximum number of retries for requests that fail with a 429 or 500-level error (default 5)
            produces: 
              - variable: http_retry_max
      - type: row
        subitems: 
          - type: checkbox
            name: interactive
            description: Enable interactive behavior. Defaults to true if the terminal supports it (default false)
            val-false: undefined
            produces: 
              - variable: interactive
      - type: row
        subitems: 
          - type: textfield
            name: output
            description: Desired output format [text|json] (default "text")
            produces: 
              - variable: output
      - type: row
        subitems: 
          - type: checkbox
            name: trace
            description: Show a log of network activity while performing a command
            val-false: undefined
            produces: 
              - variable: trace
      - type: row
        subitems: 
          - type: checkbox
            name: verbose
            description: Enable verbose output 
            val-false: undefined
            produces: 
              - variable: verbose
      - type: 'action-row'
        expandable: false
        name: doctl registry create
        consumes:
          - variable: registry_name
            required: true
          - variable: region
            parameter: --region
            required: true
          - variable: subscription_tier
            parameter: --subscription-tier
            required: true
          - variable: access_token
            parameter: --access-token
            required: false
          - variable: api_url
            parameter: --api-url
            required: false
          - variable: config
            parameter: --config
            required: false
          - variable: context
            parameter: --context
            required: false
          - variable: http_retry_max
            parameter: --http-retry-max
            required: false
          - variable: interactive
            parameter: --interactive
            required: false
          - variable: output
            parameter: --output
            required: false
          - variable: trace
            parameter: --trace
            required: false
          - variable: verbose
            parameter: --verbose
            required: false
        install: doctl registry create ${registry_name}
        produces:
          - variable: output
      - type: 'output-row'
        data: output
        content: ...