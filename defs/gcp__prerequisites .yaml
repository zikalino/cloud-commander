type: layout-form
id: root
form:
  #
  # Azure CLI
  #
  - type: fieldset
    platform: win32
    subitems:
      - $include: __git_install_step.yaml

footer:
  - type: footer
    id: footer
    subitems:
      - type: button
        id: install_button
        text: Create
