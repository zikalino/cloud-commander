#--------------------------------------------------------------------------------------------------------------
# Autogenerated by SmarterGen - do not modify this file.
#--------------------------------------------------------------------------------------------------------------
# linode-cli firewalls create
# Create a firewall
# API Documentation: 
# https://techdocs.akamai.com/linode-api/reference/post-firewalls
# 
# Example Usage: 
#   linode-cli firewalls create \
#     --label example-firewall \
#     --rules.outbound_policy ACCEPT \
#     --rules.inbound_policy DROP \
#     --rules.inbound '[{"protocol": "TCP", "ports": "22, 80, 8080, 443", 
# "addresses": {"ipv4": ["192.0.2.0/24", "198.51.100.2/32"], "ipv6": 
# ["2001:DB8::/128"]}, "action": "ACCEPT"}]' \
#     --rules.outbound '[{"protocol": "TCP", "ports": "49152-65535", "addresses":
# {"ipv4": ["192.0.2.0/24", "198.51.100.2/32"],"ipv6": ["2001:DB8::/128"]}, 
# "action": "DROP", "label": "outbound-rule123", "description": "An example 
# outbound rule description."}]'
# 
# Arguments:
#   --label (required): The Firewall's label, for display purposes only.
# 
#   --tags: An array of tags applied to this object.
# 
#   --devices.linodes: An array of Linode IDs.
#   --devices.nodebalancers: An array containing a NodeBalancer ID.
# 
#   --rules.inbound_policy (required): The default behavior for inbound traffic.
#   --rules.outbound_policy (required): The default behavior for outbound        
#   traffic.                                                                     
#   --rules.inbound (JSON): The inbound rules for the firewall, as a JSON array.
#   --rules.outbound (JSON): The outbound rules for the firewall, as a JSON      
#   array.                                                                       
# 
# 
type: layout-form
header: 
  - type: header
    title: linode firewalls create
    logo: icon.webp
form:
  - type: section
    subitems:
      - type: row
        subitems: 
          - type: textfield
            name: label (required)
            description: The Firewall's label, for display purposes only. 
            produces: 
              - variable: label
      - type: row
        subitems: 
          - type: textfield
            name: rules.inbound_policy (required)
            description: The default behavior for inbound traffic.
            produces: 
              - variable: rules.inbound_policy
      - type: row
        subitems: 
          - type: textfield
            name: rules.outbound_policy (required)
            description: The default behavior for outbound traffic.
            produces: 
              - variable: rules.outbound_policy
      - $include: az/__az_tags_list.yaml
      - type: row
        subitems: 
          - type: textfield
            name: devices.linodes
            description: An array of Linode IDs.
            produces: 
              - variable: devices.linodes
      - type: row
        subitems: 
          - type: textfield
            name: devices.nodebalancers
            description: An array containing a NodeBalancer ID. 
            produces: 
              - variable: devices.nodebalancers
      - type: row
        subitems: 
          - type: textfield
            name: rules.inbound
            description: The inbound rules for the firewall, as a JSON array.
            produces: 
              - variable: rules.inbound
      - type: row
        subitems: 
          - type: textfield
            name: rules.outbound
            description: The outbound rules for the firewall, as a JSON array.  
            produces: 
              - variable: rules.outbound
      - type: 'action-row'
        expandable: false
        name: linode firewalls create
        consumes:
          - variable: label
            parameter: --label
            required: true
          - variable: rules.inbound_policy
            parameter: --rules.inbound_policy
            required: true
          - variable: rules.outbound_policy
            parameter: --rules.outbound_policy
            required: true
          - variable: tags
            parameter: --tags
            required: false
          - variable: devices.linodes
            parameter: --devices.linodes
            required: false
          - variable: devices.nodebalancers
            parameter: --devices.nodebalancers
            required: false
          - variable: rules.inbound
            parameter: --rules.inbound
            required: false
          - variable: rules.outbound
            parameter: --rules.outbound
            required: false
        install: linode firewalls create
        verify: linode firewalls view ${label}
        produces:
          - variable: output
      - type: 'output-row'
        data: output
        content: ''