- item-types: civo-instance
  cmd: civo instances show ${id} --region ${region} -o json
  refresh: self
- item-types: civo-kubernetes
  cmd: civo kubernetes show ${id} --region ${region} --output json
  refresh: self
