- type: dropdown
  name: Select Image
  items: []
  source:
    cmd: |
      civo diskimage list --output json
    # it seems that id doesn't work anywhere
    path-id: $[*].name
    path-name: $[*].name
    cache: civo_diskimage_list
  produces:
    - variable: image_id
